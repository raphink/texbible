Full LaTeX Bible from Diatheke
==============================

To build the Bible:

* Install diatheke and the FreMartin Sword module (or adapt the `\bversion` variable);
* Install ruby;
* Type `lualatex -shell-escape bible`
